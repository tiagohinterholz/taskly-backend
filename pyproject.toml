[project]
name = "taskly-api"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.139.2",
    "uvicorn[standard]==0.51.0",
]

[dependency-groups]
dev = [
    "pip-audit==2.10.1",
]
